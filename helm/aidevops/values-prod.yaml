image:
  tag: latest

toolService:
  replicas: 3
  resources:
    requests:
      memory: "256Mi"
      cpu: "200m"
    limits:
      memory: "512Mi"
      cpu: "1000m"

mcpWechat:
  replicas: 1

ingress:
  host: aidevops.example.com
  tls: true

hpa:
  toolService:
    minReplicas: 3
    maxReplicas: 20
    cpuUtilization: 60
